<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tavernial">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tavernial">
        <activity android:name=".menu.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BDD.login" />
        <activity android:name=".page_jeux.page_jeux"/>
        <activity android:name=".menu.start"/>
        <activity android:name=".menu.Compte"/>
        <activity android:name=".menu.ajoutjeu"/>
        <activity android:name=".page_jeux.page_jeux2"/>
    </application>

</manifest>